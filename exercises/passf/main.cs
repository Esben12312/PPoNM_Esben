using static System.Math;
using static System.Console;

class main {
	
	static void Main() {
	int k;
		for(k=1; k<4; k++) {
		WriteLine($"List for k={k}");
			passf.make_table(x => Sin(x), 0, 2*PI, PI/4);
		}
	}
} 